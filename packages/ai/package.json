{
  "name": "@tavo/ai",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "eval:real": "vitest run src/eval.real.test.ts"
  },
  "dependencies": {
    "zod": "^3.25.28"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
