# Gitleaks (MIT). No path-wide exemptions for application or migration SQL.
title = "tavo"

[extend]
useDefault = true

# Dependency trees and VCS internals only — never entire source files.
[allowlist]
paths = [
  '''(^|/)node_modules/''',
  '''(^|/)\.git/''',
]
